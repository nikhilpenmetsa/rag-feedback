{
  "name": "ai-chat-interface",
  "version": "1.0.0",
  "description": "A simple chat interface for AI conversations",
  "main": "index.js",
  "scripts": {
    "start": "http-server ./public -p 3000 -c-1",
    "build": "cp -r public/* dist/"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.1048.0"
  }
}